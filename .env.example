# Simulated processing delay in seconds (default: 2.0)
PROCESSING_DELAY=2.0

# Idempotency key TTL in seconds — 86400 = 24 hours (industry standard)
IDEMPOTENCY_KEY_TTL=86400

# Background cleanup interval in seconds (default: 3600 = 1 hour)
CLEANUP_INTERVAL=3600
